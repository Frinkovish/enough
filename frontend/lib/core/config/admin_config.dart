/// Single hardcoded admin account for now — no roles table yet. Real
/// access control lives in Supabase RLS (see docs/admin migration SQL);
/// this constant only gates which UI entry points are shown.
const adminEmail = 'mohamedrejeb28@gmail.com';
